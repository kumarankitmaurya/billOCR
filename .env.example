# Set one or both API keys. The strategy module will auto-select
# the best available provider, or the user can pick from the UI.
GEMINI_API_KEY=your_gemini_api_key_here
GROQ_API_KEY=your_groq_api_key_here
